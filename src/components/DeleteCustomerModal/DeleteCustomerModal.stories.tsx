import { Meta, StoryObj } from '@storybook/react';
import { DeleteCustomerModal } from '.';

const title = 'organism/DeleteCustomerModal';

export default {
  title,
  component: DeleteCustomerModal
} as Meta;

export const Default: StoryObj = {
  args: {}
};
