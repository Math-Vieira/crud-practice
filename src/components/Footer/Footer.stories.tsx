import { Meta, StoryObj } from '@storybook/react';
import { Footer } from '.';

const title = 'layout/Footer';

export default {
  title,
  component: Footer
} as Meta;

export const Default: StoryObj = {
  args: {}
};
