import { Meta, StoryObj } from '@storybook/react';
import { NoData } from '.';

const title = 'svg/NoData';

export default {
  title,
  component: NoData
} as Meta;

export const Default: StoryObj = {
  args: {}
};
