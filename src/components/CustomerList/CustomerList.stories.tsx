import { Meta, StoryObj } from '@storybook/react';
import { CustomerList } from '.';

const title = 'Organism/CustomerList';

export default {
  title,
  component: CustomerList
} as Meta;

export const Default: StoryObj = {
  args: {}
};
