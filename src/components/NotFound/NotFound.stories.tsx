import { Meta, StoryObj } from '@storybook/react';
import { NotFoundBody } from '.';

const title = 'svg/NotFoundBody';

export default {
  title,
  component: NotFoundBody
} as Meta;

export const Default: StoryObj = {
  args: {}
};
