import { Meta, StoryObj } from '@storybook/react';
import { CustomerModal } from '.';

const title = 'organism/CustomerModal';

export default {
  title,
  component: CustomerModal
} as Meta;

export const Default: StoryObj = {
  args: {}
};
