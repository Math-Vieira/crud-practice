import { Meta, StoryObj } from '@storybook/react';
import { View } from '.';

const title = 'svg/View';

export default {
  title,
  component: View
} as Meta;

export const Default: StoryObj = {
  args: {}
};
