import { Meta, StoryObj } from '@storybook/react';
import { Edit } from '.';

const title = 'svg/Edit';

export default {
  title,
  component: Edit
} as Meta;

export const Default: StoryObj = {
  args: {}
};
