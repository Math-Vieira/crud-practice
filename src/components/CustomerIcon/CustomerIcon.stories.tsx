import { Meta, StoryObj } from '@storybook/react';
import { CustomerIcon } from '.';

const title = 'svg/CustomerIcon';

export default {
  title,
  component: CustomerIcon
} as Meta;

export const Default: StoryObj = {
  args: {}
};
