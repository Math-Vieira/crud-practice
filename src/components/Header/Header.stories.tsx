import { Meta, StoryObj } from '@storybook/react';
import { Header } from '.';

const title = 'layout/Header';

export default {
  title,
  component: Header
} as Meta;

export const Default: StoryObj = {
  args: {}
};
