import { Meta, StoryObj } from '@storybook/react';
import { Modal } from '.';

const title = 'molecules/Modal';

export default {
  title,
  component: Modal
} as Meta;

export const Default: StoryObj = {
  args: {}
};
